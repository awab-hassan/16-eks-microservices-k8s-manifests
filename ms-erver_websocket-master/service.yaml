apiVersion: v1
kind: Service
metadata:
  labels:
    app: ms-livewebsocket
    release: ms-livewebsocket
  name: ms-livewebsocket-service
  namespace: backend
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: ms-livewebsocket
    release: ms-livewebsocket
  sessionAffinity: None
  type: ClusterIP
